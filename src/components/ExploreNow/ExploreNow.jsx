import React from "react";
import "./style.css";

export const ExploreNow = ({ className }) => {
  return (
    <div className={`explore-now ${className}`}>
      <div className="text-wrapper">Explore Now!</div>
    </div>
  );
};
